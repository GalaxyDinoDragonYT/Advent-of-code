
file1 = open("input.txt", "a")

def order(n, d, hp):
  lookFor = {"forward", "up", "down"}
	if n == str:
    if n any(map(str.isdigit, lookFor)):
      d = d + numbers = int(word) for word in a_string.split() if word.isdigit()


with open("numbers.txt") as f:
      depth = 0
      horizontal_position = 0
			line = f.read().splitlines()
			i = 1001
      b=0
			while i > 1:
				de = depth
				i = i - 1
        hp = horizontal_position
				order(line[b], de, hp)
        b = b + 1
				order(line[b], de, hp)
